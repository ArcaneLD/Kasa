import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));

//Render the React App to the client
root.render(
	<React.StrictMode>
		<App  />
	</React.StrictMode>
);

